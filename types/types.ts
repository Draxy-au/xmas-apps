export type Notification = {
  title: string;
  status: string;
  text: string;
}

export type UserAccount = {
  id: string;
  username: string;
  email: string;
  password?: string;
}

export type SSList = {
  
}